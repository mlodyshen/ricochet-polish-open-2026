@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

:root {
  /* Colors - Ricochet Dutch Theme */
  --bg-primary: rgba(255, 255, 255, 0.6);
  /* Semi-transparent for glass effect */
  --bg-secondary: rgba(243, 244, 246, 0.6);
  /* Light Gray */

  --text-primary: #111827;
  /* Dark Gray/Black */
  --text-secondary: #4b5563;
  /* Medium Gray */
  --text-tertiary: #9ca3af;

  /* Accents - Red and Gray */
  --accent-pink: #dc2626;
  /* Red */
  --accent-cyan: #6b7280;
  /* Gray (was Cyan) */
  --accent-green: #16a34a;
  /* Green */

  --accent-primary: var(--accent-pink);
  --accent-secondary: var(--accent-cyan);

  --border-color: #e5e7eb;
  /* Light border */

  /* Glass effects */
  --glass-bg: rgba(255, 255, 255, 0.9);
  --glass-border: rgba(0, 0, 0, 0.08);

  /* Shadows */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 0 15px rgba(220, 38, 38, 0.1);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 0 25px rgba(0, 0, 0, 0.05);

  --card-gradient: linear-gradient(145deg, #ffffff, #f9fafb);
}

/* DARK MODE VARIABLES */
[data-theme='dark'] {
  --bg-primary: rgba(17, 24, 39, 0.85);
  /* Dark Slate / Black Glass */
  --bg-secondary: rgba(31, 41, 55, 0.8);

  --text-primary: #f9fafb;
  /* Gray-50 */
  --text-secondary: #9ca3af;
  /* Gray-400 */
  --text-tertiary: #6b7280;

  --border-color: rgba(255, 255, 255, 0.1);

  --accent-pink: #ef4444;
  /* Brighter Red for dark mode */
  --accent-cyan: #3b82f6;
  /* Blue for dark mode */

  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.5);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.5);
}

/* Global Background Gradient (Fixed) */
body::before {
  content: "";
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: -10;
  /* Dutch Gradient: Light Mode */
  background: linear-gradient(135deg,
      rgba(220, 38, 38, 0.15) 0%,
      rgba(255, 255, 255, 0.9) 30%,
      rgba(240, 248, 255, 0.8) 50%,
      rgba(33, 70, 139, 0.35) 100%);
  pointer-events: none;
}

[data-theme='dark'] body::before {
  /* Dutch Gradient: Dark Mode */
  background: linear-gradient(135deg,
      rgba(69, 10, 10, 0.5) 0%,
      #0f172a 30%,
      #1e293b 60%,
      rgba(23, 37, 84, 0.6) 100%);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Inter', sans-serif;
  background-color: transparent;
  /* Allow gradient to show */
  color: var(--text-primary);
  transition: background-color 0.3s ease, color 0.3s ease;
  overflow-x: hidden;
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
}

#root {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

h1,
h2,
h3,
h4,
h5,
h6 {
  color: var(--text-primary);
  font-weight: 700;
  letter-spacing: -0.025em;
}

a {
  text-decoration: none;
  color: inherit;
  transition: color 0.2s ease;
}

button {
  cursor: pointer;
  border: none;
  background: none;
  font-family: inherit;
}

/* Utilities */
.glass {
  background: var(--glass-bg);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  border: 1px solid var(--glass-border);
}

.card {
  background: var(--bg-secondary);
  border-radius: 12px;
  border: 1px solid var(--border-color);
  box-shadow: var(--shadow-sm);
  transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s;
}

.card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
  border-color: var(--accent-primary);
  /* Default hover accent */
}

/* Alternating Accents Helper Classes */
/* Usage: .accent-pink or .accent-cyan */
.accent-pink {
  --local-accent: var(--accent-pink);
}

.accent-cyan {
  --local-accent: var(--accent-cyan);
}

.border-accent-pink {
  border-color: var(--accent-pink) !important;
}

.border-accent-cyan {
  border-color: var(--accent-cyan) !important;
}

.text-accent-pink {
  color: var(--accent-pink) !important;
}

.text-accent-cyan {
  color: var(--accent-cyan) !important;
}

.bg-accent-pink {
  background-color: var(--accent-pink) !important;
  color: white;
}

.bg-accent-cyan {
  background-color: var(--accent-cyan) !important;
  color: var(--bg-primary);
  font-weight: 600;
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 1.5rem;
}

.flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
}

.text-gradient {
  background: linear-gradient(135deg, var(--accent-pink), var(--accent-cyan));
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* Scrollbar */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background-color: var(--border-color);
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background-color: var(--text-secondary);
}

/* Animations */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-in {
  animation: fadeIn 0.4s ease-out forwards;
}

/* Common Components Updates */
.btn-primary {
  background: var(--accent-pink);
  color: white;
  padding: 0.5rem 1rem;
  border-radius: 6px;
  font-weight: 600;
  transition: filter 0.2s;
}

.btn-primary:hover {
  filter: brightness(1.1);
}

/* Different button variants if needed, e.g. .btn-cyan */
.btn-cyan {
  background: var(--accent-cyan);
  color: var(--bg-primary);
  font-weight: 600;
}